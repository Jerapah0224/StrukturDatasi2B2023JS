/*      nomor 1
const readLineSync = require('readline-sync');

        let a = parseFloat(readLineSync.question('masukkan nilai untuk a: '));
        let b = parseFloat(readLineSync.question('masukkan nilai untuk b: '));
        let c = parseFloat(readLineSync.question('masukkan nilai untuk c: '));

        let hitung = a + b - c ;

        console.log("Jumlah nilai a + b - c : " + hitung);
        
*/

// nomor 2
const readLineSync = require('readline-sync');

        let t = parseFloat(readLineSync.question('masukkan nilai untuk t: '));
        let y = parseFloat(readLineSync.question('masukkan nilai untuk y: '));

        let hitung = t + y ** 2 ;

        console.log("Jumlah nilai t + y ** 2 : " + hitung);

/*      nomor 3
const readLineSync = require('readline-sync');

        let a = parseFloat(readLineSync.question('masukkan nilai untuk a: '));
        let b = parseFloat(readLineSync.question('masukkan nilai untuk b: '));
        let c = parseFloat(readLineSync.question('masukkan nilai untuk c: '));
        let d = parseFloat(readLineSync.question('masukkan nilai untuk d: '));

        let hitung = (a * b ** 2) / c + d ** 2;

        console.log("Jumlah nilai (a * b ** 2) / c + d ** 2 : " + hitung);
 */